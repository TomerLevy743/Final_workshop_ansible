---

- name: Install {{ service }} of version {{ version }}
  ansible.builtin.dnf:
     name: "{{ item }}"
     state: present
  loop: "{{ packages }}"

# - name: start service
#   ansible.builtin.service:
#     name: "{{ service }}"
#     enabled: yes
#     state: started

