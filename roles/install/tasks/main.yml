---

- name: Install {{ service }} of version {{ version }}
  package:
     name: "{{ item }}"
     state: present
  loop: "{{ packages }}"
  become: yes

# - name: start service
#   ansible.builtin.service:
#     name: "{{ service }}"
#     enabled: yes
#     state: started

